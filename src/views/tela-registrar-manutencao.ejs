<!DOCTYPE html>
<html>
<head><title>Manutenção</title></head>
<body style="font-family: sans-serif; padding: 20px;">
    <h2>CSU03 - Registrar Manutenção</h2>
    
    <div style="background: #fff3cd; padding: 20px; border-radius: 8px;">
        <form action="/manutencao" method="POST">
            <label>Rastreador com defeito:</label><br>
            <select name="rastreadorId" required style="width: 300px; padding: 8px;">
                <% rastreadores.forEach(r => { %>
                    <option value="<%= r.id %>"><%= r.imei %> - <%= r.modelo %></option>
                <% }) %>
            </select><br><br>

            <label>Descrição do Problema:</label><br>
            <textarea name="descricao" rows="3" cols="40" required placeholder="Descreva o defeito..."></textarea><br><br>

            <label>Técnico Responsável:</label><br>
            <select name="tecnico" required style="padding: 8px; width: 300px;">
                <option value="">-- Selecione o Técnico --</option>
                <% tecnicos.forEach(t => { %>
                    <option value="<%= t.nome %>"><%= t.nome %></option>
                <% }) %>
            </select><br><br>

            <button type="submit" style="padding: 10px 20px;">Abrir Ordem de Serviço</button>
        </form>
    </div>
    <br><a href="/">Voltar ao Painel</a>
</body>
</html>