<!DOCTYPE html>
<html>
<head>
    <title>SIGRA - Login</title>
    <style>
        body { font-family: 'Segoe UI', sans-serif; background: #e9ecef; display: flex; justify-content: center; align-items: center; height: 100vh; margin: 0; }
        .login-card { background: white; padding: 40px; border-radius: 8px; box-shadow: 0 4px 10px rgba(0,0,0,0.1); width: 300px; text-align: center; }
        input { width: 100%; padding: 10px; margin: 10px 0; border: 1px solid #ddd; border-radius: 4px; box-sizing: border-box; }
        button { width: 100%; padding: 10px; background: #007bff; color: white; border: none; border-radius: 4px; cursor: pointer; font-size: 16px; }
        button:hover { background: #0056b3; }
        .erro { color: red; font-size: 0.9em; margin-bottom: 10px; }
        .logo { font-size: 2em; font-weight: bold; color: #333; margin-bottom: 20px; display: block;}
    </style>
</head>
<body>
    <div class="login-card">
        <span class="logo">🛰️ SIGRA</span>
        
        <% if (erro) { %>
            <div class="erro">E-mail ou senha incorretos!</div>
        <% } %>

        <form action="/login" method="POST">
            <input type="email" name="email" placeholder="E-mail" required>
            <input type="password" name="senha" placeholder="Senha" required>
            <button type="submit">Entrar</button>
        </form>

        <div style="margin-top: 20px; font-size: 0.8em; color: #666; text-align: left;">
            <strong>Teste Rápido:</strong><br>
            admin@sigra.com / 123<br>
            operador@sigra.com / 123<br>
            tecnico@sigra.com / 123
        </div>
    </div>
</body>
</html>